// módulo 2: USOS derivados - sites de chamada nascidos do mesmo nome
// escrito que gerou as funções (o rename cruza módulos sem soletrar
// nome gerado)
#include "ppm.ch"

PROCEDURE Main()

   DISPARA Salva
   DISPARA Envia
   ? EVENTO Pronto
   ? Freio_Roda()

   RETURN
