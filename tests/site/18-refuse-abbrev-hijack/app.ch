// Classic #command (the dBase family): the head also matches ABBREVIATED,
// from four letters on - so ROTULA is also matched by ROTU and ROTUL.
#command ROTULA <t> => Registra( <t>, 0 )

#command PAUTAR <x> => Registra( <x>, 1 )
